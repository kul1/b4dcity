<%= form_for(@mytestapp) do |f| %>
  <% if @mytestapp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mytestapp.errors.count, "error") %> prohibited this mytestapp from being saved:</h2>

      <ul>
      <% @mytestapp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
